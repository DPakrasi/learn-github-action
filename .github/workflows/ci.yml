name: Learn Github Action
on:
  push:
    branches:
      - main

jobs:
  npm-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
      # - name: Create Github Tag
      #   run: |
      #     git config --global user.email "pakrasideepsa@gmail.com"
      #     git config --global user.name "DPakrasi"
      #     git tag -a "2.0.0" -m "Creating tag 2.0.0"
      #     git push origin "2.0.0"
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: 2.0.0
